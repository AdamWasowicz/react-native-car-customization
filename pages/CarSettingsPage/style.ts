import { StyleSheet } from "react-native";


const style = StyleSheet.create({
    root: {
        flex: 1,
        padding: 16,
    },

    center: {
        textAlign: 'center',
    },

    button: {
        marginBottom: 16,
    },

    buttonLast: {
        marginBottom: 32,
    }
})

export default style;